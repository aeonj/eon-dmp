package eon.hg.fap.core.domain;

/**
 * @author apple
 */
public enum LogType {
    LOGIN, LOGOUT, LIST, VIEW, INSERT, UPDATE, DELETE, SAVE, REMOVE, RESTORE, ERASE, CHECK, CANCEL, RETURN, SEND, PRINT, IMPORT, EXPORT
}